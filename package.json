{
  "scripts": {
    "start": "live-server --port=5500",
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "live-server": "^1.2.2"
  }
}
